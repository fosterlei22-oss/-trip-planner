# 🎯 AI 应用开发实习 · 面试题库（35 题）

> 配套项目：智能旅行助手（LLM 多 Agent 应用）
> 用法：先自己答，再看答案。答案要能**口头说出来**，不是背下来。

---

## 一、项目类（必考，先讲 3 分钟项目）

**1. 讲讲你的项目？**
> 用 README 里的 3 分钟讲演稿。结构：一句话定位 → 架构（编排器+三 Agent）→ 每个 Agent 的核心技术（RAG、Function Calling、确定性计算）→ 流式 → 降级 → 踩过的坑。

**2. 为什么拆成三个 Agent？为什么按"研究→行程→预算"编排？**
> 职责单一：研究管"选哪些景点"、行程管"怎么排"、预算管"多少钱"。编排顺序是**硬性数据依赖**——排行程必须有景点列表，算预算必须有每天的安排（门票是遍历 day_plans 算的）。前一个输出是后一个输入，这就是 pipeline。好处：每个模块可单独替换、可单独测试。

**3. 你的 Agent 和"直接调一次 LLM"有什么区别？**
> 直接调用是"问一句答一句"。Agent 是多轮推理：① 有明确的子任务划分；② 能调用工具获取外部信息；③ 有状态（前一个 Agent 的输出传给下一个）；④ 每一步可观测（SSE 进度）。本质是"把复杂任务分解成多步，每步由 LLM 决策 + 代码执行"。

**4. 你项目的 RAG 流程是什么？解决了什么？**
> 流程：40 个景点 → 拼成文档 → 向量化（自研特征哈希）→ 存库 → 用户查询向量化 → 余弦相似度召回 top-k（粗筛）→ 交 LLM 精排去噪。解决的是：真实场景景点几千个，全塞进 prompt 会超长、烧 token、效果差。RAG 让模型只看到相关的 top-k，还顺便防幻觉（从库里取，不凭空编）。

**5. Function Calling 是怎么实现的？为什么 LLM 需要工具？**
> 实现：定义工具 schema（OpenAI function 格式）→ 调用模型带 tools 参数 → 模型返回 tool_calls（要调哪个工具+参数）→ 代码执行 → 结果以 tool 消息喂回 → 循环直到模型输出最终答案（ReAct）。为什么需要：LLM 只能"想"不能"算"和"查"——比如算两个景点间真实距离，它不会也没数据，必须调用工具。这就是"LLM 决策，代码执行"。

**6. 哪些用 LLM、哪些用确定性代码？为什么？**
> 研究、行程用 LLM（推理型任务）；预算用确定性代码（计算型任务）。理由：LLM 不擅长精确算术、按 token 收费、输出不可复现（账单不能每次不一样）；代码精确、免费、可测试。选型原则：**推理交给 LLM，计算交给代码**。

**7. 怎么防幻觉？**
> 三层：① 只允许 LLM 从候选列表选（prompt 约束）；② 返回的名字与真实数据核对（`by_name.get()`），不在库里直接过滤；③ 校验失败整层降级（抛异常退回规则引擎）。核心认知：**prompt 是软约束，代码校验才是硬保障**。

**8. 项目里最大的坑？**
> 两个都值得讲。① 模型会把 JSON 包在 markdown 代码块里，直接 `json.loads` 会崩 → 我写了稳健的 `extract_json`（去围栏→截取 {} 区间）。② 神经 embedding 模型下载太慢（国内网络）→ 我实现了自研特征哈希向量化，接口不变，可平滑升级。这种"遇到问题→分析→解决"的过程面试官最想听。

**9. 为什么不直接用 LangChain / 现成框架？**
> 为了理解底层原理：手写 ReAct 循环（~30 行）让我彻底搞懂 tool calling 的消息流，手写向量检索让我搞懂 embedding 是什么。框架是工具，先懂原理再上框架，面试官更看重后者。而且自研实现可控、无额外依赖。（如果面试官问"你会用框架吗"，答：会，理解了原理后能快速上手。）

**10. 项目要上线，还缺什么？**
> 并发控制（LLM 调用加限流/队列）、缓存（相同请求复用结果）、鉴权（API key 放服务端）、数据库持久化、监控日志、部署（Docker + 云）、评测集。答出"并发+鉴权+缓存+部署"就够加分。

---

## 二、LLM 基础

**11. 什么是 token？上下文超了怎么办？**
> token 是模型处理文本的基本单位，中文约 1 字≈1-2 token。上下文窗口是单次能处理的最大 token 数。超了：截断旧内容、压缩历史、换更大窗口模型、分块检索（RAG）。

**12. temperature 是什么？**
> 控制输出的随机性。低（0-0.3）→ 确定、稳定，适合结构化输出；高（0.8-1）→ 多样、有创意。我的项目用 0.3，因为要稳定的 JSON 结果。

**13. 一句话解释 Transformer 的注意力机制？**
> 让模型在生成每个词时，动态关注输入中"相关"的词，而不是机械地看整个句子。相关性通过学习得到。这是 LLM 能理解长上下文语义的核心。

**14. 什么是 embedding？和 one-hot 有什么区别？**
> embedding 是把文本映射成稠密向量，语义相近的文本向量距离近（"猫"和"狗"近，"猫"和"汽车"远）。one-hot 是稀疏向量，维度=词表大小，且词与词完全正交、无语义。embedding 低维、稠密、有语义。

**15. 流式输出怎么实现？**
> 后端：`stream=True` 逐 chunk 返回 LLM 输出，用 SSE（text/event-stream）或 WebSocket 推给前端。前端：`fetch` 的 `response.body.getReader()` 逐段读，或 `EventSource`。我项目里做的是**阶段级流式**（每个 Agent 跑完推一个进度事件），因为输出是结构化 JSON，token 级流无意义。

**16. 怎么控制成本和延迟？**
> ① prompt 精简（少废话就少 token）；② 缓存相同请求；③ 小模型干简单活（分类用 qwen-turbo，复杂推理用大模型）；④ 批量请求；⑤ 检索只喂 top-k 不喂全量。面试说出"prompt 精简 + 缓存 + 分模型"就够了。

**17. 模型怎么选型？**
> 看 4 个维度：能力（复杂推理 vs 简单任务）、成本（deepseek-chat 便宜、能力够用）、速度/延迟、数据安全（能不能出网/私有化）。国内常用：DeepSeek（便宜均衡）、Qwen（中文强）、GLM（工具调用稳）、GPT-4o/Claude（能力上限）。我选 DeepSeek 因为 OpenAI 兼容 + 便宜，换模型只改一处配置。

---

## 三、Function Calling / Agent

**18. function calling 底层是什么？**
> 不是模型"真的执行"了函数。是模型在训练时学会了：遇到需要外部信息时，输出一个结构化的"调用请求"（工具名+参数 JSON），**由调用方代码去真正执行**，再把结果以 tool 消息传回，模型基于结果继续推理。本质上是一种"受约束的对话协议"。

**19. ReAct 是什么？Plan-and-Execute 呢？**
> ReAct = Reasoning + Acting：模型"思考→调用工具→看结果→再思考"，循环交替。Plan-and-Execute：先让模型输出完整计划（要调哪些工具、什么顺序），再一次性执行，不中途回传。ReAct 更灵活适合动态任务，Plan-and-Execute 更省 token 适合步骤固定的任务。我项目里行程 Agent 用的就是 ReAct。

**20. 单 Agent 还是多 Agent？**
> 任务简单→单 Agent 更快更省；任务复杂、可拆成独立子任务→多 Agent 各司其职更清晰、可并行。我的项目任务明确拆成研究/行程/预算三段，数据依赖清晰，所以多 Agent。注意：**多 Agent 不是越多越好**，编排复杂度和 token 成本会翻倍。

**21. 怎么防止工具循环死循环？**
> ① 设置最大轮数（我 `chat_with_tools` 里 `max_rounds=5`，超了抛异常）；② 工具执行加 try/except，出错返回 error 而不是让模型重试；③ 校验工具参数合法性。面试答出"max_rounds 上限"就有分。

**22. Agent 的记忆怎么做？**
> 短期记忆：把对话历史放进 messages（有 token 上限，需压缩）。长期记忆：向量库存储用户偏好，下次检索注入。还可以用"记忆 Agent"定期总结历史。我项目暂未做记忆，但这是可扩展点。

---

## 四、RAG

**23. RAG 解决了什么问题？**
> ① **幻觉**：模型不知道你私有数据，RAG 让它基于检索到的资料回答；② **知识过期**：模型训练数据有截止日期；③ **成本**：全量知识塞 prompt 太贵，只喂相关部分。一句话：**把答案需要的知识检索出来放进 prompt**。

**24. 怎么切块（chunking）？chunk 多大？**
> 规则：按语义边界切（段落/句子），不要从中间切碎；chunk 大小看文档和查询，一般几百 token。太小丢失上下文、太大噪音多、超出模型窗口。经验做法：先按段落切，必要时 overlap（重叠几个字避免切断语义）。项目里我每个景点本身就是一块（天然语义单元）。

**25. 怎么评估检索质量？**
> ① 指标：召回率（recall@k，答案文档在不在前 k 里）、命中率；② 人工：拿几个典型 query 看 top-k 结果是否合理；③ 端到端：看最终 LLM 回答质量。我的项目是人工 + 典型 query 抽查。

**26. 检索效果不好怎么优化？**
> 从前往后排查：① 换更好的 embedding 模型（中文用 bge-m3）；② 加粗排/重排（cross-encoder 重排 top-k）；③ 混合检索（BM25 关键词 + 向量，取并集再重排）；④ 调 chunk 大小；⑤ 丰富文档内容（元数据过滤）。说出"换 embedding + 重排 + 混合检索"就够。

**27. 向量库怎么选型？**
> FAISS（库，轻量，内存）、Chroma（简单，本地，适合原型）、Milvus（分布式，海量数据）、pgvector（直接用 PostgreSQL，省一套系统）。原型用 Chroma，生产数据量大用 Milvus。我项目因网络原因自研了向量检索，接口设计为可平滑切到这些库。

**28. 长文档怎么处理？**
> RAG 分段检索（不整篇喂）；或 map-reduce：先分块让模型分别总结，再合并总结。不要硬塞整篇。

---

## 五、Prompt 工程

**29. 好的 prompt 有哪些要素？**
> ① 角色（"你是资深旅行规划师"）；② 任务（"从候选里选最匹配的"）；③ 约束（"只能从列表选""同天不重复"）；④ 输出格式（"只输出 JSON"）；⑤ 示例（few-shot）。我的 system prompt 就是按这个写的，约束写得越明确，模型越听话。

**30. few-shot / CoT / 结构化输出是什么？**
> few-shot：给几个输入输出示例再让模型干活。CoT（思维链）：让模型先推理再回答（"让我们一步步想"），复杂推理准确率高。结构化输出：JSON mode / function calling，强制模型输出可解析的结构。三个都是提准确率的手段，各有适用场景。

---

## 六、后端 / 工程

**31. FastAPI 和 Flask 的区别？**
> FastAPI：基于类型注解自动生成 API 文档、自动请求校验（Pydantic）、原生支持 async、性能高（Starlette）。Flask：更轻更简单、生态老但自由度高。现代 API 服务一般选 FastAPI。

**32. Pydantic 有什么用？**
> 数据模型 + 校验 + 序列化三合一：声明字段类型和约束（`ge=1, le=14`），请求进来自动校验（不合法直接 422），返回时自动序列化成 JSON。是 FastAPI 的根基。

**33. CORS 是什么？**
> 浏览器安全机制：不同源的页面（如 5173 端口）默认不能访问别的源（8000 端口）的接口。CORS 就是服务器声明"允许哪些源访问我"。我项目里配了 `allow_origins` 允许前端源。

**34. SSE 和 WebSocket 区别？**
> SSE：单向（服务器→客户端）、基于 HTTP、自动重连、只能发文本、实现简单，适合"服务端推进度/通知"。WebSocket：双向、全双工、需要专门协议，适合聊天这类实时双向场景。我的场景是"服务端推进度"，SSE 足够且更简单。

**35. 并发变大了，你的系统哪里会先崩？**
> ① LLM API 限流（并发请求多会被 429）；② 无缓存导致重复调用烧钱变慢；③ 同步生成器阻塞。解法：限流/队列、加缓存、异步化、水平扩容。面试答出"限流+缓存"就够。

---

## 七、开放题

**36. 怎么给 LLM 应用做评估？**
> 建评测集（典型问题+标准答案）→ 跑一遍 → 指标：回答准确率、检索 recall@k、格式合法率、延迟成本。自动化（LLM 打分/规则） + 人工抽检结合。

**37. 如果让你给这个项目加一个功能，你会加什么？**
> 好的回答方向（选一个展开）：① 对话式行程调整（多轮记忆，"第三天太赶"→ 重新排）；② 实时 POI 数据接入（高德 API）；③ 多用户持久化 + 分享；④ 行程导出 PDF/日历。重点不是功能本身，而是你能讲清楚**为什么加、怎么加、技术难点在哪**。
